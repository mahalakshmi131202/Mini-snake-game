# Makefile for Mini Snake Game

# Variables
PYTHON = python3
PORT = 8000

# Default target
.PHONY: help
help:
	@echo "Mini Snake Game Commands:"
	@echo "  make run    - Start a local web server at http://localhost:$(PORT)"
	@echo "  clean       - Remove temporary system files"
	@echo "  push        - Quick git add, commit, and push (Save progress)"

# 1. Run the game locally using Python's built-in server
.PHONY: run
run:
	@echo "🐍 Starting Snake Game at http://localhost:$(PORT)..."
	$(PYTHON) -m http.server $(PORT)

# 2. Clean up junk files (Mac/Linux specific)
.PHONY: clean
clean:
	find . -type f -name "*.DS_Store" -delete
	find . -type d -name "__pycache__" -exec rm -rf {} +

# 3. Shortcut to save everything to GitHub
.PHONY: push
push:
	git add .
	git commit -m "Update game progress"
	git push origin main